---
trigger: always_on
---

1. venv is installed in ./.venv, use that python environment whenever running pip/python
2. the bot runs on a hosted environment, not locally, though a docker image is available and setup to run locally if you need to test