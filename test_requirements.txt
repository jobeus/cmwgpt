# Test requirements for Discord Bot
# These are additional packages needed for testing

# Core testing framework (usually included with Python)
# unittest is built-in, but we might want additional test utilities

# Mocking and testing utilities
pytest>=9.0.2
pytest-asyncio>=1.3.0
pytest-mock>=3.15.1
pytest-cov>=7.0.0

# For async testing
asynctest>=0.13.0

# For better test output and reporting
colorama>=0.4.6
coverage>=7.13.3

# Code quality and linting tools
flake8>=7.3.0
autopep8>=2.0.0
black>=26.1.0

# All the main dependencies (for testing against real implementations)
discord.py>=2.6.4
python-dotenv>=1.2.1
openai>=2.17.0
requests>=2.32.5
